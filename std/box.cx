struct Box<V> {
    V value;
    bool hasValue;

    static Box<V> empty() => .{hasValue = false};
    
    static Box<V> of(V val) => .{val, true};
    
    V unwrap() => self.value;
    
    bool isEmpty() => !self.hasValue;
}
