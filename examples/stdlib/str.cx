//! Hello Fernando the Dev\n
import std.string;
import std.io;

int main() {
    String s1 = .from("Fernando");
    String s2 = .from(" the ");

    defer s1.free();
    defer s2.free();

    s1.concat(s2);
    s1.concat("Dev");

    printf("Hello %s\n", s1.data());
    return 0;
}
